<form method="post" action="{{ path('app_carte_sim_delete', {'id': carte_sim.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_sim.id) }}">
    <button class="btn">Delete</button>
</form>
