<form method="post" action="{{ path('app_pending_sim_cards_delete', {'id': pending_sim_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pending_sim_card.id) }}">
    <button class="btn">Delete</button>
</form>
