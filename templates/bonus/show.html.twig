{% extends 'base.html.twig' %}

{% block title %}Bonus{% endblock %}

{% block body %}
    <h1>Bonus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bonu.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ bonu.valeur }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ bonu.motif }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ bonu.createdAt ? bonu.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bonus_index') }}">back to list</a>

    <a href="{{ path('app_bonus_edit', {'id': bonu.id}) }}">edit</a>

    {{ include('bonus/_delete_form.html.twig') }}
{% endblock %}
