{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-secondary btn-sm" href="{{ path('app_bonus_index') }}">Annuler</a>
    <button class="btn btn-primary btn-sm">{{ button_label|default('Attribuer le bonus') }}</button>

{{ form_end(form) }}
