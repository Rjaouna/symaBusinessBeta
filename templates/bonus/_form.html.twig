{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-sm">{{ button_label|default('Attribuer') }}</button>
    <a class="btn btn-secondary btn-sm" href="{{ path('app_bonus_index') }}">Annuler</a>

{{ form_end(form) }}
