<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_syma_business') }}">
      <img src="{{ asset('assets/img/logo_syma.svg') }}" alt="Logo Syma">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav ms-auto"> <!-- Ajout de la classe ms-auto -->
        {% if app.user %}
        <!-- Partie admin -->
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="#"><i class="bi bi-people"></i> Liste des clients</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_banner_index')}}"><i class="bi bi-people"></i> Gestion des banniers</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_user_index')}}"><i class="bi bi-people"></i> Gestion des utilisateur</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_quota_index')}}"><i class="bi bi-people"></i> Gestion des quota</a>
        </li>
        <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="cartesSimDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-people"></i> Cartes Sim
        </a>
        <ul class="dropdown-menu" aria-labelledby="cartesSimDropdown">
            <li>
                <a class="dropdown-item" href="{{ path('app_sim_type_index') }}">
                    <i class="bi bi-people"></i> Types carte Sim
                </a>
            </li>
            <li>
                <a class="dropdown-item" href="{{ path('app_carte_sim_index') }}">
                    <i class="bi bi-people"></i> Gestion carte Sim
                </a>
            </li>
             <li>
                <a class="dropdown-item" href="{{ path('app_carte_sim_batch') }}">
                    <i class="bi bi-people"></i> Importer un lot de carte
                </a>
            </li>
            
            
        </ul>
        </li>

        
        {% else %}
        <!-- Partie utilisateur -->
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_gestion_bonus')}}"><i class="bi bi-gift"></i> Gérer mon bonus !</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#"><i class="bi bi-sliders"></i> Gérer mon bonus !</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_profile') }}">Mon Profil</a>
        </li>
        
        {% endif %}
        {% endif %}

      </ul>
      <div class="dropdown ms-5"> <!-- Wrapper pour le menu déroulant -->
        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
          <i class="bi bi-person fs-5"></i> Mon compte
        </a>
        <div class="dropdown-menu dropdown-menu-end"> <!-- Classe pour aligner à droite -->
          {% if app.user %}
            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
            <a class="dropdown-item" href="{{ path('app_syma_business_config_index') }}">Modifier mon compte</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Administration</a>
          {% else %}
            <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>
            <a class="dropdown-item" href="{{ path('app_register') }}">Créer un compte !</a>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</nav>
