<form method="post" action="{{ path('app_quota_delete', {'id': quotum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quotum.id) }}">
    <button class="btn btn-danger my-1"><i class="bi bi-trash"></i></button>
</form>
