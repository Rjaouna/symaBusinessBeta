{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-secondary btn-sm" href="{{ path('app_quota_index') }}">Annuler</a>
    <button class="btn btn-primary btn-sm">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
